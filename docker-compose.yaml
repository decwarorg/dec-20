# docker compose  up --build --force-recreate utexas
name: project-utexas-arpanet
services:
  dec-20:
    build: ./
    image: dec-20
#    ports:
#      - "2030:2030"
    volumes:
      - type: bind
        source: ./external
        target: /external
#    command: sh -c "cd external/t20v4 && ./pdp10-kl disks-boot.ini"
# below leaves container with interactive terminal
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    